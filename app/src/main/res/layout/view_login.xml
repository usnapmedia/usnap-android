<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/view_login_signupToggle_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/action_signup"
            android:textColor="@color/dark_gray"
            android:background="@drawable/sel_login_toggle_btn"/>

        <Button
            android:id="@+id/view_login_loginToggle_btn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/action_login"
            android:textColor="@color/dark_gray"
            android:background="@drawable/sel_login_toggle_btn"/>

    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.samsao.snapzi.authentication.view.ContentLoginView
            android:id="@+id/view_login_content_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </FrameLayout>

    <Button
        android:id="@+id/view_login_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/dark_gray"
        tools:text="@string/action_login"
        android:background="@drawable/sel_login_btn"/>

</merge>